import styled, { css } from "@emotion/native";
import { View } from "react-native";

export const PartyContainer = styled.View`
  display: flex;
  margin: 0 auto;
  margin-top: 30px;
  border-radius: 20px;
  width: 90%;
  height: 70px;
  background-color: white;
  display: flex;
`;

export const PartyTextBox = styled.Text`
  display: flex;
`;

export const PartyText = styled.Text`
  font-size: 15px;
  font-weight: 900;
  color: gray;
`;

export const PartySecondText = styled.Text`
  font-size: 15px;
  font-weight: 900;
  color: gray;
`;
